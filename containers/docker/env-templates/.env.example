# Example environment for docker-compose in this directory
# Copy this file to .env and adjust values as needed.

# Docker user to run applicable containers as (format: UID:GID)
# Find your IDs using:
#   id -u   # prints your UID
#   id -g   # prints your GID
# Common example on many systems:
# DOCKER_USER=1000:1000
DOCKER_USER=1000:1000
